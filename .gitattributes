# Keep repository text deterministic across Linux, macOS, and Windows clones.
* text=auto eol=lf

# Windows-native scripts retain their platform convention.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
